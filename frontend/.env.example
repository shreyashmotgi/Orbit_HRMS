# Leave this unset for local development (Vite proxy handles /api -> http://localhost:5000).
# When deploying frontend and backend separately, set this to your deployed backend URL:
# VITE_API_URL=https://your-backend-url.onrender.com/api
